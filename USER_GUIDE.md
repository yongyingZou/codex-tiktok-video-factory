# 商品视频工厂 V2.0.0 使用指南

## 第一次使用

### 1. 准备商品目录

```text
商品ID_辅助名称/
├── product/       商品图片
├── SP/            素材视频
└── product.md     可选：你确认过的商品事实、规格和禁用说法
```

不要把生成结果放进 `product/` 或 `SP/`。工厂会把分析和成片写入商品目录下的
`analysis/` 与 `output/`。

### 2. 在 Codex 中发出第一条指令

直接复制：

```text
使用商品视频工厂 V2.0.0。
读取这个商品目录的全部图片和视频，先完成商品理解和素材分析。
选择一个方向做单视频校准，先把购买逻辑和方案给我确认，暂不批量。
目标市场：日本。
```

把商品目录拖入任务，或补充目录的绝对路径。

### 3. 确认方案后再渲染

你只需要检查：

- 商品是什么，有没有认错；
- 视频面向谁，解决什么具体问题；
- 画面是否支持口播；
- 是否使用了不能确认的参数、价格或功效；
- 这个方向是否值得先做一条校准片。

确认后说：

```text
方向确认。只渲染这一条基准视频，完成技术检查和完整视听检查后再给我结果。
```

### 4. 检查交付文件

主要文件位于：

```text
output/<市场>/
├── videos/                  成片
├── covers/                  封面
├── publish/                 每条视频的结构化发布记录
├── reports/                 渲染与质检报告
├── edit-plans/              剪辑计划
└── 发布资料_中日对照.md      可直接审核和复制的发布资料
```

有口播的视频必须包含同步烧录字幕。发布资料必须列出实际字幕、中文含义、时间和对应口播；
目标市场话题标签另有一行可整行复制。

## 发现问题时怎么说

直接描述现象和期望，不需要猜技术原因：

```text
问题：视频有口播，但是没有字幕。
期望：当前视频不用重做，但后续所有有口播的视频都必须带同步字幕。
请分别说明当前成品、通用流程、测试、Git 和安装版的处理状态。
```

处理结果应明确列出：已定位、已修改、已验证、已提交、已推送、已安装。没有完成的状态
必须写明，不能笼统称为“已更新”。

## 多市场和多语言

V2.0.0 配置了 20 个经过记录的 TikTok Shop 市场，支持日语、英语、中文、泰语、越南语、
印尼语、马来语、菲律宾语、葡萄牙语、西班牙语、法语、意大利语、德语、荷兰语、波兰语
和泰米尔语等内容语言。

日语流程经过重点校准。其他市场应先完成一条单视频校准，不应直接承诺所有语言都达到
同等本地化质量。

## 批量生产门槛

第一条基准视频必须经过完整画面和声音审核并标记为通过，才能制作同商品的其他方向。
建议连续校准不同商品后再开放批量，不要因为技术 QA 通过就直接认定可以发布。

